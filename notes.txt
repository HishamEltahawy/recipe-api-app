docker-compose run --rm app sh -c "flake8"	# remove container after end command "use for test"
docker-compose run --rm app sh -c "django-admin startproject app ."	# remove container after end command "use for test"
docker-compose up	#create container from image and run it 